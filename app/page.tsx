import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <main>
      <section className="min-h-screen">
        <Button>Click Me</Button>
      </section>
    </main>
  );
};

export default Home;
